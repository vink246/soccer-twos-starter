algorithm:
  type: ppo
  lr: 0.0003
  gamma: 0.99
  gae_lambda: 0.95
  clip_range: 0.2
  vf_coef: 0.5
  ent_coef: 0.01
  max_grad_norm: 0.5
  n_epochs: 4
  batch_size: 256
  n_steps: 2048

model:
  architecture: mlp_actor_critic
  hidden_sizes: [256, 256]

rollout:
  n_steps: 2048
